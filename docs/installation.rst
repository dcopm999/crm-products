============
Installation
============

At the command line::

    $ easy_install crm-products

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv crm-products
    $ pip install crm-products
